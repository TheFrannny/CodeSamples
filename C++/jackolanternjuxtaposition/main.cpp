#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
using namespace std;

int main() {
    ios::sync_with_stdio(false);

    int a, b, c;
    cin >> a >> b >> c;

    cout << a * b * c << endl;

    return 0;
}
